type: object
description: Current status of an evaluation job
allOf:
  - $ref: ./EvaluationJobState.yaml
  - type: object
    properties:
      benchmarks:
        type: array
        items:
          $ref: ./BenchmarkStatus.yaml
        description: Per-benchmark status
