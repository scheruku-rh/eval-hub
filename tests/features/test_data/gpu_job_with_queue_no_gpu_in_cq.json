{
  "model": {
    "url": "{{env:MODEL_URL|http://test.com}}",
    "name": "{{env:MODEL_NAME|test}}",
    "auth": {
      "secret_ref": "{{env:MODEL_AUTH_SECRET_REF|test}}"
    }
  },
  "benchmarks": [
    {
      "id": "arc_easy",
      "provider_id": "{{env:GPU_TEST_PROVIDER_ID}}",
      "parameters": {
        "num_examples": 10
      }
    }
  ],
  "name": "gpu-test-with-queue-no-gpu-in-cq",
  "tags": ["gpu", "test", "scenario-2c", "kueue", "no-gpu-quota"],
  "queue": {
    "kind": "kueue",
    "name": "cpu-local-queue"
  }
}
